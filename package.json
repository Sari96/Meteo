{
  "name": "meteo",
  "version": "1.0.0",
  "description": "Meteo",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "copy_resources": "mkdir -p ./dist && cp -R ./resources ./dist",
    "start": "npm run copy_resources && parcel index.html",
    "build": "npm run copy_resources && parcel build index.html --public-url .",
    "deploy": "npm run build && gh-pages -d dist"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/Sari96/Meteo.git"
  },
  "author": "Sarah Cappelli",
  "license": "ISC",
  "bugs": {
    "url": "https://github.com/Sari96/Meteo/issues"
  },
  "homepage": "https://github.com/Sari96/Meteo#readme",
  "dependencies": {
    "gh-pages": "^5.0.0",
    "parcel": "^2.8.3"
  },
  "devDependencies": {
    "@parcel/transformer-sass": "^2.8.3"
  }
}
